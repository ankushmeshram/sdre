package de.dfki.isreal.data;

/**
 * A state is a snapshot of the knowledge base or triple store. It is needed
 * as input for the SCP planner.
 * 
 * TODO should be checked for a better format than an ontology string.
 * 
 * @author stenes
 *
 */
public interface State{
	
	/**
	 * Returns the state as ontology string in RDF/XML syntax string.
	 * @return
	 */
	public String getOntology();

}
